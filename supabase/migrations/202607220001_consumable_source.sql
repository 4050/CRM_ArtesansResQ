-- Ділимо основний склад на 3 джерела постачання: держзакупівля, благодійна
-- допомога, інше. `source` — вільний текст того ж штибу, що і unit/category
-- (див. 202607170014): застосунок завжди пише в нього один із трьох відомих
-- кодів (через select у формі), а переклад і групування на 3 вкладки робить
-- рівень застосунку. Значення поза цим набором (вписані вручну в БД) просто
-- не потраплять у жодну з трьох вкладок, лише в загальний список "Усі" —
-- так само, як невідома category сьогодні не має перекладу.
alter table public.consumables add column source text not null default 'other';
